Since the day 2 tasks has different implementation, you can run them adding a flag to go run main.go command

If you want to run the first task:
```bash
go run main.go -task=1
```

If you want to run the second task:
```bash
go run main.go -task=2
```